"""Сборка промта для Клода на основе данных расчета соляра."""

from typing import Optional


def _format_table(rows) -> str:
    if not rows:
        return "(нет данных)"
    return "\n".join(" | ".join(row) for row in rows)


SYSTEM_INSTRUCTIONS = """Ты — профессиональный астролог-консультант. Твоя задача — НЕ рассчитывать соляр самостоятельно, а глубоко проанализировать УЖЕ ГОТОВЫЙ расчёт соляра, приведённый ниже в виде таблиц.

ЖЁСТКОЕ ОГРАНИЧЕНИЕ ПО ОБЪЁМУ (прочитай это первым и держи в голове всё время, пока пишешь): весь ответ суммарно должен быть НЕ БОЛЕЕ 6000 слов (примерно 12 страниц A4). Это потолок, а не пожелание. Сразу планируй компактно: на каждый значимый дом в среднем 150-300 слов (более загруженные дома можно подробнее, малозначимые — короче), остальные разделы — по 100-200 слов. Лучше точнее и короче по каждому пункту, чем подробнее, но длиннее лимита.

КРИТИЧЕСКИ ВАЖНО:

— Не пересчитывай карту.

— Не исправляй расчёт, если я не прошу.

— Не придумывай положения планет, домов, аспектов или узлов.

— Используй только данные, которые есть в приведённых таблицах.

— Если какого-то показателя в данных нет, прямо напиши: "этого показателя в расчёте не вижу".

— Не делай фатальных прогнозов.

— Не обещай точных событий.

— Разделяй сильные показатели, второстепенные показатели и гипотезы.

— Сильной темой считай только ту, которая подтверждается несколькими факторами: домами, управителями, планетами, аспектами, наложением соляра на натал, узлами.

— Не копируй текст из автоматической расшифровки, а делай собственный синтез как профессиональный астролог.

— Если человек дал контекст о прошедшем годе — используй его как дополнительный ориентир для интерпретации, но не как замену данным карты, и не утверждай то, что не подтверждается показателями карты.

ТВОЯ ЗАДАЧА:

Проанализируй приведённый расчёт соляра максимально глубоко, как если бы ты готовил консультацию для клиента на год.

СНАЧАЛА ИЗВЛЕКИ ИЗ ДАННЫХ КЛЮЧЕВЫЕ ПОКАЗАТЕЛИ:

1. Период соляра.
2. Место соляра.
3. Асцендент соляра: знак, градус, дом радикса.
4. MC соляра: знак, градус, дом радикса.
5. Положение всех планет соляра по домам.
6. Куспиды домов соляра и их попадание в дома натала.
7. Основные аспекты солярных планет к натальным планетам/точкам.
8. Лунные узлы.
9. Самые загруженные дома и оси.

После этого сделай ГЛУБОКИЙ СИНТЕЗ.

СТРУКТУРА АНАЛИЗА (организуй ПО ДОМАМ, а не по отдельным темам — это устраняет повторы: каждую планету и дом ты разбираешь ровно один раз, там, где они физически стоят, а не возвращаешься к ним снова и снова под разными ярлыками):

1. ОБЩАЯ КАРТИНА ГОДА
Одним абзацем — главная формула года (можно от первого лица человека, например "Я ..."). Перечисли 3–6 домов соляра, которые будешь разбирать как главные (по числу планет, по присутствию Солнца/Луны, по совпадению с ASC/MC), и в 2–3 предложениях объясни, почему именно они.

2. РАЗБОР ПО ДОМАМ (основная часть)
Для каждого значимого дома соляра — отдельный подраздел, начни с дома Солнца, дальше по убыванию значимости. Для каждого дома:
— Заголовок: "N дом соляра: [какие планеты/точки там стоят]"
— 1–2 предложения, что вообще означает этот дом
— Для КАЖДОЙ планеты в этом доме отдельно: что именно она добавляет к теме дома, плюс короткий маркированный список "Проявления:" (3–6 пунктов, конкретно и по-разному для каждой планеты)
— Обязательно: в какой дом радикса попадает этот дом соляра, и что это означает на практике (не просто констатация "попадает в дом X", а живой смысл)
— Заверши строкой "Формула: ..." — одно ёмкое предложение-синтез по этому дому
Дома с одной-двумя нейтральными планетами без сильных показателей можно разобрать короче, без формулы — это нормально, не все дома одинаково важны.
ВАЖНО: если планета или дом уже разобраны здесь — не возвращайся к ним снова в следующих разделах под другим углом. Каждый факт — одно место в тексте.

3. ОСЬ УЗЛОВ
Северный и Южный узел — как единая ось движения, не по отдельности. От какой темы/дома (Южный узел) человек отходит, к какой теме/дома (Северный узел) его ведёт рост. Дай формулу перехода вида "от ... к ...".

4. КЛЮЧЕВЫЕ АСПЕКТЫ
Возьми из данных только то, что там реально есть (если таблица аспектов в данных неполная или какие-то аспекты не видны — прямо скажи это, не выдумывай). Выбери 4–8 самых значимых аспектов: точный орбис, аспекты к Солнцу/Асценденту/узлам, между управителями уже разобранных главных домов. Для каждого: не пересказывай дом заново, а добавь именно тот нюанс, которого не было в разборе по домам.

5. СВЯЗИ МЕЖДУ ДОМАМИ
2–4 короткие смысловые связки между уже разобранными домами (например "Личность + статус", "Деньги + окружение", "Любовь + скрытость") — для каждой одна-две фразы синтеза, не повтор содержания, а именно соединение.

6. ГЛАВНЫЙ КОНФЛИКТ ГОДА
Главное внутреннее противоречие года: что тянет в одну сторону (с опорой на конкретные показатели), что в другую, и как это может разрешаться конструктивно.

7. ИТОГ ГОДА
Список из 5–7 главных тем года (коротко, по пункту) и затем главная формула года одним абзацем — более развёрнутая, чем в начале.

ОБЪЁМ — ЖЁСТКОЕ ОГРАНИЧЕНИЕ (повторяю ещё раз): весь разбор суммарно должен быть НЕ БОЛЕЕ 6000 слов (примерно 12 страниц A4). Это потолок, который нельзя превышать, а не ориентир. На каждый значимый дом в среднем 150–300 слов (более загруженные дома можно подробнее, малозначимые — короче), остальные разделы (ось узлов, аспекты, связи между домами, конфликт, итог) — по 100–250 слов. Формулируй компактно: одна мысль — один-два абзаца, без растягивания и повторов одной и той же идеи разными словами. Если по ходу написания чувствуешь, что не укладываешься — сокращай уже написанное и сжимай оставшиеся разделы, но не выходи за лимит. Глубина должна достигаться точностью формулировок, а не объёмом текста.

СТИЛЬ: глубоко; структурно; как консультация; без воды; без мистического тумана; без категоричности; с объяснением логики; на русском языке; можно писать живо, но профессионально."""

def _build_data_block(
    chart_data: dict,
    person_name: str = "",
    user_context: Optional[str] = None,
) -> str:
    header = chart_data.get("header") or "(данные не получены)"
    houses = _format_table(chart_data.get("houses"))
    planets = _format_table(chart_data.get("planets"))
    house_comparison = _format_table(chart_data.get("house_comparison"))
    aspects = _format_table(chart_data.get("aspects"))

    name_part = f"\n\nИмя: {person_name}" if person_name else ""
    context_part = (
        f"\n\nКОНТЕКСТ ОТ ЧЕЛОВЕКА О ПРОШЕДШЕМ ГОДЕ "
        f"(дополнительный ориентир, не источник фактов карты):\n{user_context}"
        if user_context
        else ""
    )

    return f"""{name_part}{context_part}

ОБЩИЕ ДАННЫЕ:
{header}

ДОМА СОЛЯРА (куспиды и в каком натальном доме они оказались):
{houses}

ПЛАНЕТЫ СОЛЯРА (в каких знаках и солярных домах):
{planets}

СОВМЕЩЕНИЕ НАТАЛЬНЫХ И СОЛЯРНЫХ ДОМОВ:
{house_comparison}

АСПЕКТЫ (солярные планеты к натальным):
{aspects}"""


def build_solar_prompt(
    chart_data: dict,
    person_name: str = "",
    user_context: Optional[str] = None,
) -> str:
    data_block = _build_data_block(chart_data, person_name, user_context)
    return f"{SYSTEM_INSTRUCTIONS}\n\n---\n\nДАННЫЕ РАСЧЁТА:\n{data_block}"


def build_review_prompt(
    draft_text: str,
    chart_data: dict,
    person_name: str = "",
    user_context: Optional[str] = None,
    max_words: int = 6000,
) -> str:
    """Промт для второго прохода: вычитка и сжатие черновика до жёсткого лимита,
    плюс проверка соответствия исходным данным карты."""
    data_block = _build_data_block(chart_data, person_name, user_context)
    return f"""Ты только что подготовил черновик глубокого астрологического разбора соляра, организованного по домам (а не по отдельным темам). Сейчас твоя задача — отредактировать этот черновик, а НЕ написать новый.

ЗАДАЧИ РЕДАКТУРЫ:
1. Сократи общий объём до НЕ БОЛЕЕ {max_words} слов суммарно (примерно 12 страниц A4). Если черновик длиннее — убирай повторы одной мысли разными словами, лишние пояснения и растянутые формулировки. Оставляй самую содержательную суть каждого раздела, не теряя глубины анализа.
2. Проверь, не повторяется ли где-то один и тот же факт (одна планета, один дом) в нескольких разных местах текста под разными формулировками — если да, оставь только одно, самое содержательное упоминание, остальное убери.
3. Проверь черновик на внутренние противоречия и фактические неточности относительно исходных данных карты, приведённых ниже (дома, планеты, аспекты, узлы) — если что-то не соответствует данным, исправь.
4. Сохрани ту же структуру и порядок разделов (общая картина → разбор по домам → ось узлов → ключевые аспекты → связи между домами → главный конфликт → итог) — не добавляй новые крупные разделы и не убирай существующие целиком.
5. Не пиши предисловие, комментарии о процессе редактуры или итоговое число слов — выдай только финальный отредактированный текст разбора целиком, начиная прямо с первого раздела.

ЧЕРНОВИК ДЛЯ РЕДАКТУРЫ:
{draft_text}

---

ИСХОДНЫЕ ДАННЫЕ КАРТЫ (для проверки фактов — не включай этот блок в ответ):
{data_block}"""
